import 'package:flutter/material.dart';

class ChallengesAndAwards extends StatelessWidget {
  const ChallengesAndAwards({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Personalised Challenges and Awards'),
      ),
    );
  }
}
