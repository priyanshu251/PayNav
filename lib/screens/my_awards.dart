import 'package:flutter/material.dart';

class MyAwards extends StatelessWidget {
  const MyAwards({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('My Status and Awards'),
      ),
    );
  }
}
